# dependencies
/node_modules

# 테스트 커버리지 결과 (테스트 실행 시 자동 생성, git에 올릴 필요 없음)
/coverage

# 빌드 결과물 (배포용 파일, git에 올릴 필요 없음)
/build
/dist

# macOS에서 자동 생성되는 시스템 파일 (불필요한 파일)
.DS_Store

# 환경변수 파일 (보안상 git에 올리지 않음)
.env*
!.env.example  # 예시 파일만 git에 포함

# 로그 파일 (에러 발생 시 자동 생성, git에 올릴 필요 없음)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# 임시 파일
*.tmp
*.temp

# IDE 설정 파일 (개인별 설정이므로 git에 올릴 필요 없음)
.vscode/
.idea/
*.swp
*.swo

# OS 생성 파일
Thumbs.db

# 업로드 파일 (사용자가 업로드한 파일들, git에 올릴 필요 없음)
/public/uploads/

# 데이터베이스 파일 (로컬 개발용, git에 올릴 필요 없음)
*.sqlite
*.db
database.sqlite
